<% [['Username', user.username], ['Email', user.email], ['Industry', user.industry], ['Visibility', visibility_status(user)]].each do |label, value| %>
  <%= render 'label_value', label:, value: %>
<% end %>
